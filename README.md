# App de Podcasts usando Next JS

![Captura diseño web](./.readme-static/image.png)

## Instructions from run on local app

Require NodeJS from run this project

* `npm i` for install the dependences.
* `npm run dev` for run the dev envorinment.
* `npm run build && npm start` for compile a production SSR.